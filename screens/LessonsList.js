import React from 'react';
import { SafeAreaView, Text } from 'react-native';

class LessonsList extends Component {
    render() {
        return(
            <SafeAreaView>
                <Text>LessonsList</Text>
            </SafeAreaView>
        );
    };
}

export default LessonsList;